# PASSAGE-WORLD-001 — LOADIN.STEAD conformance

The existing `loadinstead` m0 router is sufficient for the PASSAGE-WORLD outward hand.

Two route bits may carry the same payload to the same declared destination while preserving different exact ALEX `formation_ref` values. Route coincidence does not erase formation ancestry.

Hard boundary:

```text
route != admit
same route != same passage
formation_ref is opaque to LOADIN.STEAD
route proposal != passage-equivalence verdict
```

No PASSAGE-specific production router primitive is introduced by this conformance gate.
