interface:
  display_name: "ALEX"
  short_description: "Research source witnesses with attributable evidence paths"
  default_prompt: "Use $alex to investigate this question through attributable source witnesses. Preserve source-to-claim transformations, keep discovery trace separate from evidence, pressure load-bearing assumptions, and load only the bounded reference protocols the selected research shape requires."

policy:
  allow_implicit_invocation: true
